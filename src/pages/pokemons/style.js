import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    topContainer: {
       height: '60%',
    },
    bottomContainer: {
        height: '40%',
        marginTop: 40
    },
    bottomTitle: {
        fontSize: 28,
        fontWeight: 'bold',
    }
})

export default style;