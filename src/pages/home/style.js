import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    container: {
        flex: 1,
        padding: '5%'
    },
    headerContainer: {
        marginTop: 20
    },
    discoverText: {
        fontSize: 30,
        fontWeight: 'bold',

    }
})

export default style;